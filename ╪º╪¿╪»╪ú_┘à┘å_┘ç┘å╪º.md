# 🎯 ابدأ من هنا - دليل المبتدئين الكامل

> **لمين ده مكتوب؟** للي معندوش خبرة برمجة وعاوز يشغّل التطبيق على Firebase بدون تثبيت أي حاجة على الكمبيوتر.

> **هياخد قد إيه؟** 30-45 دقيقة بس، ومتفصلش لحد ما تخلّص.

> **مين هيدفع؟** كله مجاناً! Firebase مجاني للاستخدام الصغير، GitHub Codespaces بيدّيك 60 ساعة مجاناً شهرياً.

---

## 📋 اللي هتحتاجه قبل ما تبدأ

تأكد إن عندك:

- ✅ **حساب Google** (Gmail) - عشان Firebase
- ✅ **حساب GitHub** - مجاني، اعمله من [github.com/signup](https://github.com/signup) لو معندكش
- ✅ **متصفح حديث** (Chrome أو Edge أو Firefox)
- ✅ **اتصال إنترنت ثابت**

**مش هتحتاج:** Node.js، VSCode، Git، أو أي برنامج تاني. كل حاجة هتشتغل في المتصفح!

---

## 🚀 المرحلة 1: فتح Codespace (5 دقايق)

### الخطوة 1.1: اعمل Fork للمشروع

1. افتح هذا الرابط في المتصفح:
   ```
   https://github.com/mgcair1/M-Group-Cool-
   ```

2. اضغط زر **"Fork"** في أعلى يمين الصفحة

   ![](https://docs.github.com/assets/cb-79255/mw-1440/images/help/repository/fork-button.webp)

3. اضغط **"Create fork"** في الصفحة اللي هتفتح

4. هيتعمل عندك نسخة من المشروع باسم:
   ```
   https://github.com/[اسم_حسابك]/M-Group-Cool-
   ```

### الخطوة 1.2: افتح Codespace

1. في صفحة الـ Fork اللي عملته، اضغط زر **"Code"** الأخضر

2. اختار تاب **"Codespaces"**

3. اضغط **"Create codespace on main"**

   هيفتحلك صفحة فيها VSCode في المتصفح. **استنى دقيقة-اتنين** لحد ما يحمّل.

4. **مهم:** لما يخلّص التحميل، هيشتغل تلقائياً:
   ```
   npm install
   ```
   استنى لحد ما يخلّص (هتشوف ✓ في الـ Terminal).

---

## 🔥 المرحلة 2: إعداد Firebase (15 دقيقة)

### الخطوة 2.1: إنشاء مشروع Firebase

1. افتح في تاب جديد: [console.firebase.google.com](https://console.firebase.google.com/)

2. سجّل دخول بحساب Google بتاعك

3. اضغط **"Add project"** أو **"إضافة مشروع"**

4. اكتب اسم: `m-group-cool` (أو أي اسم تختاره)

5. **عطّل** Google Analytics (مش محتاجها)

6. اضغط **"Create project"** واستنى دقيقة

### الخطوة 2.2: فعّل خدمات Firebase الثلاثة

#### A) Authentication (المصادقة)

1. من القائمة اليسارية اضغط **"Build"** → **"Authentication"**
2. اضغط **"Get started"**
3. اختار **"Email/Password"**
4. فعّل أول خيار بس (Email/Password)
5. اضغط **"Save"**

#### B) Firestore Database (قاعدة البيانات)

1. من القائمة اليسارية اضغط **"Build"** → **"Firestore Database"**
2. اضغط **"Create database"**
3. اختار **"Start in production mode"** (مهم!)
4. اختار Location:
   - **مصر/السعودية:** اختار `eur3 (europe-west)`
5. اضغط **"Enable"** واستنى دقيقة

#### C) Storage (تخزين الملفات)

1. من القائمة اليسارية اضغط **"Build"** → **"Storage"**
2. اضغط **"Get started"**
3. اختار **"Start in production mode"**
4. اختار نفس الـ Location (eur3)
5. اضغط **"Done"**

### الخطوة 2.3: ربط Web App

1. ارجع للصفحة الرئيسية للمشروع (Project Overview)

2. اضغط على أيقونة **`</>`** (Web)

3. اكتب اسم: `M Group Cool`

4. **متفعّلش** "Firebase Hosting" (هنفعّله من Terminal)

5. اضغط **"Register app"**

6. **مهم جداً:** هيظهر كود JavaScript. انسخ الجزء ده فقط:
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSy...",
     authDomain: "your-project.firebaseapp.com",
     projectId: "your-project",
     storageBucket: "your-project.appspot.com",
     messagingSenderId: "...",
     appId: "..."
   };
   ```
   احفظه في مكان مؤقت (Notepad مثلاً) - هنستخدمه دلوقتي.

---

## 💻 المرحلة 3: النشر من Codespace (10 دقايق)

### الخطوة 3.1: تحديث ملف الإعدادات

1. ارجع لتاب Codespace في المتصفح

2. في الـ Files panel (يسار)، افتح:
   ```
   src/firebase-applet-config.json
   ```

3. امسح كل محتواه واكتب (مع البيانات اللي نسختها):
   ```json
   {
     "apiKey": "حط_قيمة_apiKey_هنا",
     "authDomain": "your-project.firebaseapp.com",
     "projectId": "your-project",
     "storageBucket": "your-project.appspot.com",
     "messagingSenderId": "حط_messagingSenderId",
     "appId": "حط_appId",
     "firestoreDatabaseId": "(default)"
   }
   ```

4. احفظ الملف (`Ctrl+S`)

### الخطوة 3.2: شغّل التطبيق

في الـ Terminal تحت، اكتب:

```bash
npm install -g firebase-tools
```

استنى لحد ما يخلّص (دقيقة)

ثم:

```bash
firebase login --no-localhost
```

**مهم:** هيطلعلك:
1. اضغط `Y` لما يسألك
2. هيدّيك **رابط**: انسخه وافتحه في المتصفح
3. وافق على الصلاحيات
4. هيدّيك **كود**: انسخه وارجع للـ Terminal والصقه

ثم:

```bash
firebase use --add
```

اختار المشروع اللي عملته في Firebase (m-group-cool) واديله alias اسمه `default`.

### الخطوة 3.3: انشر التطبيق

اكتب الأمر ده وانتظر:

```bash
npm run build
firebase deploy
```

استنى **3-5 دقايق** لحد ما يخلّص. في الآخر هيظهرلك:

```
✔  Deploy complete!

Project Console: https://console.firebase.google.com/...
Hosting URL: https://m-group-cool.web.app
```

🎉 **مبروك! التطبيق دلوقتي شغّال على الإنترنت!**

---

## 👤 المرحلة 4: إنشاء حساب أول مشرف (5 دقايق)

### الطريقة الأسهل: من Firebase Console

1. افتح: [Firebase Console](https://console.firebase.google.com/) → مشروعك

2. اضغط **"Authentication"** → **"Users"**

3. اضغط **"Add user"**

4. اكتب:
   - **Email:** `admin@example.com` (أو بريدك)
   - **Password:** كلمة مرور قوية (8 أحرف على الأقل)

5. اضغط **"Add user"**

6. انسخ الـ **User UID** اللي ظهر (شكله طويل وعشوائي)

### الآن اربط الحساب بصلاحية super_admin:

1. اضغط **"Firestore Database"** من القائمة

2. اضغط **"Start collection"**

3. اكتب اسم الـ Collection: `users`

4. اضغط **"Next"**

5. في **"Document ID"** الصق الـ **UID** اللي نسخته

6. أضف الحقول التالية (اضغط "Add field" لكل واحد):

   | Field | Type | Value |
   |---|---|---|
   | `uid` | string | (نفس الـ UID) |
   | `email` | string | `admin@example.com` |
   | `name` | string | `الإدارة العامة` |
   | `role` | string | `super_admin` |
   | `needsPasswordChange` | boolean | `false` |

7. اضغط **"Save"**

---

## 🎉 المرحلة 5: استخدم التطبيق!

### على الكمبيوتر:

1. افتح المتصفح
2. روح للرابط اللي ظهر في الخطوة 3.3:
   ```
   https://m-group-cool.web.app
   ```
3. سجّل دخول بالبريد وكلمة المرور

### على الموبايل:

#### طريقة 1: افتحه في المتصفح
1. افتح Chrome أو Safari في موبايلك
2. اكتب نفس الرابط: `https://m-group-cool.web.app`
3. سجّل دخول

#### طريقة 2: ثبّته كتطبيق على الموبايل (PWA)
1. افتح الرابط في Chrome (Android) أو Safari (iPhone)
2. اضغط زر **القائمة** (النقاط الثلاث في Android، زر المشاركة في iPhone)
3. اضغط **"Add to Home screen"** أو **"إضافة إلى الشاشة الرئيسية"**
4. سيظهر التطبيق كأيقونة على شاشة موبايلك!
5. افتحه واستخدمه زي أي تطبيق عادي 📱

---

## 🔄 ازاي تعدّل أو تحدّث التطبيق

### لما تحب تعدّل أي حاجة:

1. ارجع لـ Codespace (نفس الرابط من قبل)

2. عدّل الكود اللي عاوز تعدّله

3. في Terminal اكتب:
   ```bash
   npm run deploy
   ```

4. التحديث هيوصل لكل المستخدمين فوراً!

### لو طلبت مني تعديلات:

1. ابعتلي اللي عاوز تعدّله
2. أنا هاعمل التعديلات في الكود
3. هابعتلك الـ commits
4. تعمل `git pull` في Codespace
5. تشغّل `npm run deploy`

---

## ❓ المشاكل الشائعة

### ❌ "Firebase project not found"
```bash
firebase use --add
# اختار المشروع تاني
```

### ❌ "Login required"
```bash
firebase login --reauth
```

### ❌ التطبيق ما اشتغلش (Page not found)
- تأكد من نسخ الـ config بطريقة صحيحة
- شغّل: `npm run build && firebase deploy` تاني

### ❌ "Permission denied" لما بحاول أعمل حاجة في التطبيق
- تأكد إنك أضفت document في `users` بـ role = "super_admin"
- تأكد إن الـ UID صح

### ❌ Codespace فصل أو وقف
- مفيش مشكلة! روح [github.com/codespaces](https://github.com/codespaces)
- ارجع لنفس الـ Codespace أو افتح جديد

---

## 💡 نصائح مهمة

### ✅ افعل:
- 🔐 **احفظ كلمة مرور المشرف** في مكان آمن
- 📱 **اعمل bookmark** للرابط في موبايلك
- 💾 **اعمل نسخة احتياطية** كل أسبوع من داخل التطبيق:
  - الإعدادات → الخزينة الاحتياطية → تصدير
- 👥 **أضف موظفيك** من داخل التطبيق (وحدة System Administration)

### ❌ لا تفعل:
- ❌ ما تشاركش بيانات تسجيل الدخول مع حد
- ❌ ما تكتبش `firebase logout` (هتحتاج تسجّل دخول تاني)
- ❌ ما تحذفش مشروع Firebase

---

## 🆘 لو حصلت مشكلة

### تواصل معايا في أي وقت بـ 3 معلومات:

1. **رابط مشروعك على Firebase:**
   ```
   https://console.firebase.google.com/project/[اسم_مشروعك]
   ```

2. **رابط الـ Fork بتاعك:**
   ```
   https://github.com/[حسابك]/M-Group-Cool-
   ```

3. **Screenshot من الـ Terminal** لو فيه error

أنا موجود لمساعدتك في أي مشكلة! 🤝

---

## 📊 ملخص الأوامر اللي هتستخدمها

| الأمر | الوظيفة |
|---|---|
| `firebase login --no-localhost` | تسجيل دخول Firebase (مرة واحدة) |
| `firebase use --add` | اختيار المشروع (مرة واحدة) |
| `npm run build` | بناء التطبيق |
| `firebase deploy` | نشر التطبيق |
| `npm run deploy` | بناء + نشر في خطوة واحدة |
| `git pull` | جلب آخر التحديثات |

---

**🎯 جاهز للبدء؟ ابدأ من المرحلة 1!**

لو فيه أي خطوة مش فاهمها، **اسأل دلوقتي** قبل ما تستمر.
