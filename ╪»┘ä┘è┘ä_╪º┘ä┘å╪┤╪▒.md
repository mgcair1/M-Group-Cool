# 🚀 دليل نشر M Group Cool على Firebase

دليل كامل خطوة بخطوة لنشر التطبيق على الإنترنت واستخدامه فعلياً مع فريق عملك.

---

## 📋 الفهرس

1. [المتطلبات](#1-المتطلبات)
2. [إعداد حساب Firebase](#2-إعداد-حساب-firebase)
3. [النشر التلقائي (الأسهل)](#3-النشر-التلقائي-الأسهل)
4. [النشر اليدوي خطوة بخطوة](#4-النشر-اليدوي-خطوة-بخطوة)
5. [إنشاء حسابات المستخدمين](#5-إنشاء-حسابات-المستخدمين)
6. [النسخ الاحتياطية](#6-النسخ-الاحتياطية)
7. [التحديثات والتعديلات](#7-التحديثات-والتعديلات)
8. [حل المشاكل الشائعة](#8-حل-المشاكل-الشائعة)

---

## 1. المتطلبات

قبل ما تبدأ، تأكد إن عندك:

- ✅ **Node.js 18 أو أحدث** — [نزّل من nodejs.org](https://nodejs.org)
- ✅ **حساب Google** — للتسجيل في Firebase
- ✅ **بطاقة ائتمانية** — (اختياري) فقط لو هتطلع الخطة المدفوعة لاحقاً. الخطة المجانية كافية للبداية.

تأكد من Node.js:
```bash
node --version    # لازم يطلع v18.0.0 أو أعلى
npm --version     # لازم يطلع 9.0.0 أو أعلى
```

---

## 2. إعداد حساب Firebase

### الخطوة 2.1: إنشاء مشروع جديد

1. افتح [Firebase Console](https://console.firebase.google.com/)
2. اضغط **"Add project"** (إضافة مشروع)
3. اكتب اسم المشروع، مثلاً: `m-group-cool-prod`
4. اضغط **Continue**
5. **عطّل Google Analytics** (مش محتاجها الآن، تقدر تفعّلها بعدين)
6. اضغط **Create project** واستنى دقيقة

### الخطوة 2.2: تفعيل Authentication

1. من القائمة الجانبية اضغط **Build → Authentication**
2. اضغط **Get started**
3. اختار **Email/Password**:
   - فعّل الخيار الأول (Email/Password)
   - **متفعّلش** Email link (passwordless)
   - اضغط **Save**
4. (اختياري) فعّل **Google** كمان لتسجيل دخول سهل:
   - اضغط **Add new provider → Google**
   - فعّله، اختار البريد، اضغط Save

### الخطوة 2.3: تفعيل Firestore Database

1. من القائمة الجانبية اضغط **Build → Firestore Database**
2. اضغط **Create database**
3. اختار **Start in production mode** (مش test mode!)
4. اختار **Location** الأقرب لك:
   - **مصر/الشرق الأوسط:** `eur3 (europe-west)` أو `nam5 (us-central)`
   - **السعودية/الإمارات:** `asia-south1` أو `eur3`
5. اضغط **Enable**

> ⚠️ **مهم:** الـ Location لا يمكن تغييره بعد الإنشاء! اختار بعناية.

### الخطوة 2.4: تفعيل Storage

1. من القائمة الجانبية اضغط **Build → Storage**
2. اضغط **Get started**
3. اختار **Start in production mode**
4. اختار نفس الـ Location اللي اخترته لـ Firestore
5. اضغط **Done**

### الخطوة 2.5: إضافة Web App

1. ارجع للـ **Project Overview** (الصفحة الرئيسية)
2. اضغط على أيقونة **</>** (Web)
3. اكتب اسم: `M Group Cool Web App`
4. **متفعّلش** "Firebase Hosting" (هنفعّله بعدين من الـ CLI)
5. اضغط **Register app**
6. **انسخ الـ config** اللي ظهر — هتحتاجه بعد شوية، شكله كده:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "m-group-cool-prod.firebaseapp.com",
  projectId: "m-group-cool-prod",
  storageBucket: "m-group-cool-prod.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```

---

## 3. النشر التلقائي (الأسهل) ⚡

عندك سكريبت جاهز يعمل كل حاجة تلقائياً:

```bash
cd /path/to/M-Group-Cool-
npm install
npm run setup:firebase
```

السكريبت هيسألك:
1. تسجيل دخول Google
2. اختيار/إنشاء مشروع
3. لصق الـ config اللي نسخته من الخطوة 2.5
4. تأكيد تفعيل الخدمات

وبعدها هينشر التطبيق تلقائياً. ✨

**النتيجة:** هيطلعلك رابط زي:
```
🌐 https://m-group-cool-prod.web.app
🌐 https://m-group-cool-prod.firebaseapp.com
```

---

## 4. النشر اليدوي خطوة بخطوة

لو عاوز تفهم كل خطوة، أو السكريبت التلقائي ما اشتغلش:

### الخطوة 4.1: تثبيت Firebase CLI

```bash
npm install -g firebase-tools
firebase --version    # تأكد من التثبيت
```

### الخطوة 4.2: تسجيل الدخول

```bash
firebase login
```

هيفتح المتصفح، سجّل دخول بحساب Google.

### الخطوة 4.3: ربط المشروع

```bash
cd /path/to/M-Group-Cool-
firebase use --add
```

اختار المشروع اللي عملته في الخطوة 2.1.

### الخطوة 4.4: تحديث ملف الـ config

افتح ملف `src/firebase-applet-config.json` والصق فيه القيم من الخطوة 2.5:

```json
{
  "apiKey": "AIzaSy_paste_here",
  "authDomain": "your-project.firebaseapp.com",
  "projectId": "your-project",
  "storageBucket": "your-project.appspot.com",
  "messagingSenderId": "123456789",
  "appId": "1:123456789:web:abc123",
  "firestoreDatabaseId": "(default)"
}
```

### الخطوة 4.5: بناء التطبيق

```bash
npm install
npm run build
```

لازم يخلّص بدون أي أخطاء. لو فيه أخطاء، شغّل:
```bash
npx tsc --noEmit
```

### الخطوة 4.6: نشر القواعد الأمنية

```bash
firebase deploy --only firestore:rules,firestore:indexes,storage
```

### الخطوة 4.7: نشر التطبيق

```bash
firebase deploy --only hosting
```

النتيجة:
```
✔  Deploy complete!

Hosting URL: https://your-project.web.app
```

افتح الرابط في المتصفح! 🎉

---

## 5. إنشاء حسابات المستخدمين

### الطريقة الأولى: من Firebase Console (يدوي)

1. افتح [Firebase Console → Authentication → Users](https://console.firebase.google.com/)
2. اضغط **Add user**
3. أدخل البريد وكلمة المرور
4. **مهم:** بعد إنشاء الحساب، لازم تضيفه يدوياً في Firestore:
   - افتح **Firestore Database**
   - أنشئ collection اسمها `users` (لو مش موجودة)
   - أنشئ document بنفس الـ `uid` بتاع المستخدم
   - أضف الحقول:
     ```
     uid: "نفس_uid"
     email: "user@example.com"
     name: "اسم الموظف"
     role: "super_admin"  ← أو admin/manager/technician/etc
     needsPasswordChange: false
     ```

### الطريقة الثانية: السكريبت التلقائي (الأسرع) ⭐

#### الإعداد لمرة واحدة:

1. افتح [Firebase Console → Project Settings → Service Accounts](https://console.firebase.google.com/)
2. اضغط **Generate new private key**
3. هينزّل ملف JSON
4. **سمّيه** `firebase-service-account.json` وحطه في مجلد المشروع
5. ⚠️ **مهم جداً:** الملف ده **محمي تلقائياً في `.gitignore`** — ماتشاركوش مع حد ولا ترفعه على GitHub!

#### إنشاء أول مشرف:

```bash
npm run create-admin
```

السكريبت هيسألك:
- البريد الإلكتروني
- كلمة المرور
- الاسم الكامل
- رقم الهاتف (اختياري)

وهينشئ الحساب بصلاحية `super_admin` تلقائياً.

#### إنشاء موظفين بصلاحيات أقل:

افتح التطبيق، سجّل دخول كـ super_admin، وأضف الموظفين من واجهة النظام (وحدة **System Administration**).

---

## 6. النسخ الاحتياطية 💾

النسخ الاحتياطي مهم جداً لبيانات شركة حقيقية!

### Backup يدوي (لما تحتاج):

```bash
npm run backup
```

هيحفظ كل البيانات في:
```
backups/backup-2026-06-02T20-30-00.json
```

والسكريبت بيحتفظ بآخر 30 نسخة تلقائياً ويحذف الأقدم.

### Backup تلقائي يومي (مهم!):

#### على Linux/Mac:
شغّل:
```bash
crontab -e
```

أضف السطر ده (يعمل backup كل يوم الساعة 2 صباحاً):
```
0 2 * * * cd /path/to/M-Group-Cool- && /usr/local/bin/npm run backup >> /var/log/mgc-backup.log 2>&1
```

#### على Windows:
استخدم **Task Scheduler**:
1. افتح Task Scheduler
2. Create Basic Task
3. اسمه: `M Group Cool Daily Backup`
4. Trigger: Daily at 02:00
5. Action: Start a program
   - Program: `C:\Program Files\nodejs\npm.cmd`
   - Arguments: `run backup`
   - Start in: `C:\path\to\M-Group-Cool-`

### استرجاع نسخة احتياطية:

من داخل التطبيق:
1. سجّل دخول كـ super_admin
2. افتح **الإعدادات → الخزينة الاحتياطية**
3. اضغط **استيراد نسخة احتياطية**
4. اختار ملف الـ JSON

---

## 7. التحديثات والتعديلات 🔄

### بعد أي تعديل على الكود:

```bash
# 1) تأكد إن الكود سليم
npm run lint

# 2) شغّل الاختبارات
npm run audit:all

# 3) ابني وانشر
npm run deploy
```

السكريبت `npm run deploy` بيعمل:
- `npm run build`
- `firebase deploy --only hosting`

كل التحديث بياخد دقيقة-اتنين، والمستخدمين هيشوفوا التحديث فوراً (لكن لازم يـ refresh).

### تحديث القواعد الأمنية فقط:

لو عدّلت `firestore.rules` أو `storage.rules`:
```bash
npm run deploy:rules
```

### نشر كل حاجة (Hosting + Rules + Storage):

```bash
npm run deploy:full
```

---

## 8. حل المشاكل الشائعة 🔧

### ❌ "Firebase project not found"
```bash
firebase use --add
# اختار المشروع تاني
```

### ❌ "Permission denied" في Firestore

تأكد إن:
1. المستخدم مسجّل دخول
2. عنده وثيقة في `users/{uid}` تحتوي على `role`
3. القواعد الأمنية متفعّلة:
   ```bash
   firebase deploy --only firestore:rules
   ```

### ❌ التطبيق بيظهر صفحة بيضاء بعد النشر

افتح Developer Tools (F12) → Console، وشوف الأخطاء.

أشهر الأسباب:
- **`firebase-applet-config.json` فاضي أو غلط** → تأكد من القيم
- **Authentication مش متفعّل** → ارجع للخطوة 2.2
- **Firestore database مش معمول** → ارجع للخطوة 2.3

### ❌ "Module not found" بعد التحديث

```bash
rm -rf node_modules package-lock.json
npm install
npm run build
```

### ❌ تجاوزت حدود الخطة المجانية

الخطة المجانية (Spark) كافية لـ:
- 50,000 قراءة Firestore/يوم
- 20,000 كتابة Firestore/يوم
- 1 GB تخزين
- 10 GB bandwidth/شهر

لو الفريق صغير (2-10 موظفين)، ده أكتر من كفايتك.

لو تجاوزت، الترقية لخطة **Blaze (Pay-as-you-go)** بتكلف:
- ~$0.18 لكل 100,000 قراءة إضافية
- معظم الشركات الصغيرة بتدفع أقل من $5/شهر

### ❌ نسيت كلمة المرور للمشرف

```bash
npm run create-admin
# أعد إنشاء نفس البريد بكلمة مرور جديدة
# السكريبت هيسألك "تحديث صلاحياته؟" قول y
```

أو من Firebase Console → Authentication → Users → اختار المستخدم → Reset password.

---

## 📞 الدعم والمتابعة

لو احتجت أي مساعدة في:
- 🐛 إصلاح مشاكل
- ✨ إضافة ميزات جديدة
- 🔄 تحديثات
- 🎨 تخصيصات في الواجهة
- 🌐 ترجمة للإنجليزي
- 📱 عمل تطبيق موبايل

**ارجعلي في أي وقت** على نفس المحادثة، أو افتح محادثة جديدة وحط رابط المشروع `https://github.com/mgcair1/M-Group-Cool-`.

كل التعديلات اللي عملتها موجودة في فرع `bugfix/full-system-repair` على GitHub، وكل الـ audit scripts موجودة في مجلد `audit/` عشان تقدر تتأكد إن أي تعديل جديد ما كسرش حاجة.

---

## ✅ Checklist النشر النهائي

- [ ] Node.js 18+ مثبت
- [ ] حساب Firebase معمول
- [ ] مشروع Firebase تم إنشاؤه
- [ ] Authentication مفعّل (Email/Password)
- [ ] Firestore database منشأة في production mode
- [ ] Storage مفعّل
- [ ] Web app مسجّل والـ config محفوظ في `src/firebase-applet-config.json`
- [ ] `firebase-service-account.json` منزّل ومحفوظ بأمان
- [ ] `npm install` تم بنجاح
- [ ] `npm run build` تم بدون أخطاء
- [ ] `firebase deploy` تم بنجاح
- [ ] أول مشرف تم إنشاؤه عبر `npm run create-admin`
- [ ] تسجيل الدخول بنجاح من الرابط الجديد
- [ ] تجربة إضافة عميل وحفظه (للتأكد من Firestore)
- [ ] إعداد cron job للنسخ الاحتياطي اليومي
- [ ] (اختياري) ربط دومين شخصي

---

**🎉 مبروك! التطبيق دلوقتي شغّال على الإنترنت ومحفوظ في السحابة.**
